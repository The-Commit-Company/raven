import { View } from 'react-native'
import { Text } from '@components/nativewindui/Text'

const ParticipatingThreads = () => {
    return (
        <View>
            <Text>ParticipatingThreads</Text>
        </View>
    )
}

export default ParticipatingThreads